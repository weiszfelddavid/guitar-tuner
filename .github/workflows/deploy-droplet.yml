name: Deploy to DigitalOcean Droplet

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 138.197.70.108
          username: root
          key: ${{ secrets.DROPLET_SSH_KEY }}
          script: |
            cd /root/guitar-tuner
            # Force git to match the repository exactly
            git fetch --all
            git reset --hard origin/main
            git clean -fd
            
            # Copy files to the standard web root
            # (Assuming Nginx/Apache looks at /var/www/html)
            rm -rf /var/www/html/*
            cp -r /root/guitar-tuner/* /var/www/html/
            
            echo "Files copied to /var/www/html!"

